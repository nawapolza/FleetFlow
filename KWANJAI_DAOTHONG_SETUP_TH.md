# ขวัญใจดาวทองขนส่ง — คู่มือรุ่นบัญชีรายเที่ยว

## สิ่งที่เพิ่ม

- หน้า **บัญชีรายเที่ยว** (เฉพาะ Admin ตามสิทธิ์เดิมของระบบ): วันที่ ทะเบียนรถ วัสดุที่ขน (ทราย/หิน/ดิน/ปูน/อื่น ๆ), น้ำหนัก/จำนวน, ต้นทาง ปลายทาง ระยะทางที่กรอกเอง, เลขใบงาน และหมายเหตุ
- ค่าขนส่งต่อเที่ยว และต้นทุนแยกเป็นค่าซื้อทราย ค่าซื้อหิน ค่าน้ำมัน ค่ายาง ค่าอะไหล่ ค่าแรงช่างซ่อม ค่าแรงคนขับ ค่าใช้จ่ายอื่น ๆ
- คำนวณรายจ่ายรวมและกำไร/ขาดทุนต่อเที่ยวด้วยหน่วยจำนวนเต็ม **สตางค์** ทั้งที่ Backend และในข้อมูล MongoDB
- รายงานรายเดือนและรายปี **แยกทะเบียนรถ** และรายเดือนในปีที่เลือก; ส่งออก CSV ตามตัวกรอง
- เพิ่ม แก้ไข ลบเที่ยวได้, ค้นรายการแยกสาขา, จำกัด CRUD ด้วยสิทธิ์ Admin ที่ Backend
- โลโก้ใหม่ใช้บนแถบเมนู หน้าเข้าสู่ระบบ หน้า Home (Dashboard) ใบสรุปงานเดิม ใบสรุป PNG และไอคอนสำหรับติดตั้งเว็บบนหน้าจอมือถือ (PWA)

## ขอบเขตของกำไร/ขาดทุน

ยอดกำไร/ขาดทุน = **ค่าขนส่งที่กรอก − ต้นทุนตรงทั้ง 8 หมวดที่กรอกต่อเที่ยว** ไม่ใช่กำไรสุทธิทางบัญชีหากยังไม่ได้กรอกค่าเสื่อมราคา ภาษี หรือค่าใช้จ่ายอื่นทั้งหมด ช่องว่างในหมวดต้นทุนจะถือเป็น 0 บาท

**ข้อมูลบัญชีรายเที่ยวเก็บใน collection `trip_finance` แยกจาก `transport_ledger` และ `deliveries` เดิม** เพื่อไม่ให้รวมรายรับ/รายจ่ายเดิมซ้ำ ผู้ใช้ต้องกรอกค่าขนส่งและค่าใช้จ่ายในหน้าใหม่นี้ด้วยตนเอง ข้อมูลเก่าไม่ได้ถูกคัดลอกหรือคำนวณทดแทน

## เริ่มต้นในเครื่อง (Windows)

1. ติดตั้ง Node.js 20 และสร้างไฟล์ `backend/.env` จาก `backend/.env.example` ใส่ MongoDB Atlas URI และ JWT_SECRET/ADMIN_RECOVERY_KEY ของคุณเอง ห้ามใส่ค่าเหล่านี้ใน GitHub
2. เปิด CMD ใน `backend` แล้ว `npm ci` จากนั้น `npm start`
3. เปิด CMD อีกหน้าต่างใน `frontend` แล้ว `npm ci` จากนั้น `npm run dev`
4. เพิ่มรถในเมนู **รถและคนขับ** ก่อน เลือก **บัญชีรายเที่ยว** เพื่อบันทึกข้อมูล
5. `/adminnakub` ยังเป็นเส้นทางกู้คืนบัญชีเดิม ต้องใช้ ADMIN_RECOVERY_KEY

## Deploy บน Render

Frontend Static Site: Root Directory `frontend` · Build Command `npm ci --include=dev && npm run build` · Publish Directory `dist` · Rewrite `/*` → `/index.html`.
Backend Web Service: Root Directory `backend` · Start Command `npm start` · ตั้ง Environment Variables ของ MongoDB และ secret ใน Render เท่านั้น ไม่เผยแพร่ไฟล์ `.env`.
Frontend ให้ตั้ง `VITE_API_URL` เป็น URL ของ Backend เป็น URL ของ Backend โดย **ไม่ต้องต่อท้าย `/api`** เมื่อ Deploy บน Render; สำหรับ Local ให้ใช้ `VITE_API_URL=/api` ซึ่ง Vite proxy จะตัด `/api` ให้อัตโนมัติ.

ระบบเก็บข้อมูลแยกตามสาขาและสิทธิ์ Admin เดิม ไม่ได้ทดสอบเชื่อม MongoDB Atlas ของคุณจริงในสภาพแวดล้อมนี้ โปรดทดลองเพิ่ม/แก้ไข/ลบรายการด้วยฐานข้อมูลทดสอบก่อนใช้จริง และทำสำเนาฐานข้อมูลก่อน Deploy
