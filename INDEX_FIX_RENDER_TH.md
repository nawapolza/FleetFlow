# แก้ Render Backend: MongoDB IndexKeySpecsConflict (code 86)

## สาเหตุ
ฐานข้อมูลเดิมมี `users.username_1` แบบ `unique: true, sparse: true` แต่ Backend พยายามสร้างดัชนีชื่อเดียวกันแบบ `unique: true` โดยไม่มี sparse จึงเกิด code 86 และการเตรียมฐานข้อมูลล้มเหลว แม้ Render จะแสดง service live ก็ตาม

## สิ่งที่แก้
- เพิ่ม `backend/indexCompatibility.js` เพื่อตรวจดัชนีเดิมก่อนเรียกสร้างดัชนี 23 รายการที่เริ่มต้นระบบ
- สำหรับ `username_1` ที่เดิม `unique+sparse` จะใช้ของเดิม โดยไม่ลบ/สร้างใหม่ และไม่แตะบัญชี Admin
- ไม่ปิดการตรวจ username ซ้ำ: ถ้าดัชนีเดิมไม่ unique จะหยุดพร้อมข้อความชัดเจนเพื่อให้ตรวจข้อมูลอย่างปลอดภัย
- มีชุดทดสอบ `backend/test/indexCompatibility.test.js`

## ติดตั้ง
1. สำรองข้อมูล MongoDB ก่อนอัปเดตระบบที่ใช้ข้อมูลจริง
2. แตก ZIP แล้วอัปโหลดโค้ดใหม่ขึ้น GitHub (ไม่อัปโหลด `.env`)
3. Render: Deploy Backend ใหม่ก่อน แล้วทดสอบ `/health-ready` ของ Backend ตามที่มีในโปรเจกต์
4. ทดสอบ Login, `/adminnakub`, เพิ่ม/แก้ไขรถ และบันทึกงานจริงด้วยรายการทดสอบ
5. ถ้าหน้าเว็บแสดงปัญหาเดิม ตรวจว่า Frontend `VITE_API_URL` ชี้ไป Backend ที่ Deploy ใหม่ และฐานข้อมูลที่ใช้อยู่คือฐานเดิม

การแก้ครั้งนี้มุ่งแก้ดัชนีชนกันใน Logs ที่ส่งมา ไม่สามารถรับประกันว่าการบันทึกทุกหน้าใน MongoDB Atlas จริงจะผ่านได้จนกว่าจะทดสอบการเชื่อมต่อและข้อมูลจริงของคุณ
