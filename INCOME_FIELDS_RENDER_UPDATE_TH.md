# แก้ช่องรายได้งานขนส่งบนมือถือ

ในหน้า บันทึกงาน > รายได้งานที่ 1 และทุกงาน เปลี่ยนเป็น 3 ช่องตามลำดับ: ค่าทราย / ค่าหิน / ค่าบรรทุก แทน ค่าเที่ยว / เบี้ยเลี้ยง / รายได้อื่น พร้อมคงสูตรรวมรายได้เดิมไว้

## สำคัญ: ให้เว็บไซต์แสดงโค้ดเวอร์ชันนี้
- แตก ZIP นี้ และคัดลอกไฟล์ใหม่ทั้งโปรเจกต์ลงโฟลเดอร์ที่เชื่อม GitHub แล้วตรวจสอบ `frontend/src/components/DeliveryForm.jsx` มีข้อความ `label="ค่าทราย"`, `label="ค่าหิน"`, `label="ค่าบรรทุก"` ไม่ใช่ `label="ค่าเที่ยว"`.
- `git add . && git commit -m "Fix delivery income labels in mobile form" && git push origin main`
- Render > Static Site (Frontend) > Deploy latest commit; ตรวจใน Deploy Logs ว่าเป็น commit ล่าสุด และ build สำเร็จ
- เปิดหน้าใหม่/รีเฟรชแบบไม่ใช้ cache หรือเปิดใน browser private หากเป็นแอปติดตั้งบนหน้าจอหลัก ให้ปิดและเปิดใหม่ หากยังแสดงคำเดิมให้ลบ shortcut เก่าแล้วเพิ่มใหม่
- ตรวจว่าลิงก์ที่ใช้งานเป็น Render Static Site เดียวกับที่เพิ่ง Deploy

หมายเหตุ: ระบบคงชื่อฟิลด์ในฐานข้อมูลเดิมเพื่อป้องกันการสูญเสียข้อมูลย้อนหลัง การเปลี่ยนป้ายชื่อไม่ทำให้ความหมายของรายการเก่าถูกเปลี่ยนจริง โปรดตรวจสอบรายการเก่าก่อนนำไปสรุปบัญชี
