# TEST SYSTEM — ระบบบันทึกรับจ่ายงานขนส่ง

## เริ่มใช้

1. ติดตั้ง Node.js 20 และ MongoDB (หรือใช้ MongoDB Atlas)
2. คัดลอก `backend/.env.example` เป็น `backend/.env`; กรอก `MONGODB_URI`, `MONGODB_DB=test_system_db` และสุ่ม `JWT_SECRET` ที่ปลอดภัย ห้ามอัปโหลดไฟล์ `.env` ขึ้น Git
3. เปิด `INSTALL_WINDOWS.bat` (หรือที่โฟลเดอร์หลักใช้ `npm run install:all` ต้องเชื่อมอินเทอร์เน็ตเพื่อติดตั้งแพ็กเกจ)
4. เปิด `START_LOCAL_WINDOWS.bat` หรือใช้สองหน้าต่าง CMD เรียก `npm run dev:backend` และ `npm run dev:frontend`
5. สร้างบัญชี Owner ตามไฟล์ README และเพิ่มทะเบียนรถในเมนู “รถและคนขับ” ก่อน
6. ในเมนู “บัญชีงานขนส่ง” เลือกเดือน เลือกทะเบียน กรอกวันที่ วัสดุที่ขน และบันทึกรายรับ/รายจ่าย แล้วดูสรุปต่อคันได้

## รายการรับ–จ่าย

รายรับ: ค่าขนส่ง พร้อมวัสดุที่ขน (หิน ทราย ดิน ฯลฯ) จำนวนหรือหน่วย และหมายเหตุ

รายจ่าย: น้ำมัน อะไหล่ ค่าแรงช่างซ่อม ค่ายาง ค่าทางด่วน ค่าแรงคนขับ ค่าใช้จ่ายอื่น

สรุป: รายรับรวมลบรายจ่ายรวมเท่ากับคงเหลือ/กำไรหรือขาดทุน **เฉพาะรายการที่กรอกในเดือนและสาขานั้น** ไม่ใช่เงินสดจริง หรือกำไรสุทธิหลังต้นทุนที่ยังไม่ได้บันทึก

## หมายเหตุสำคัญ

- โมดูลใหม่เก็บใน `transport_ledger` และ **ไม่รวม** ค่าน้ำมัน/ค่าเที่ยวจากหน้าเก่าเข้ากับกำไรอัตโนมัติ เพื่อหลีกเลี่ยงยอดซ้ำ
- ไม่รวมคงเหลือสะสมยกมาจากเดือนก่อน: คงเหลือที่แสดงคือรายรับหักรายจ่ายของเดือนที่เลือก
- ข้อมูลเก่าไม่ถูกลบ แต่ถ้าเปลี่ยนไปใช้ฐานข้อมูล MongoDB ใหม่ จะไม่เห็นข้อมูลเดิมจนกว่าจะย้ายข้อมูลด้วยตนเอง
- มี API `GET /api/transport-ledger?month=YYYY-MM`, `POST /api/transport-ledger`, `PUT /api/transport-ledger/:id`, `DELETE /api/transport-ledger/:id` เฉพาะบัญชี Owner
- สรุปรายงานจะจัดแยกตาม `branch_id` และ `vehicle_id` เพื่อไม่ปะปนระหว่างสาขา/รถ

## การตรวจสอบที่ทำในสภาพแวดล้อมนี้

ตรวจโครงสร้าง JSX และ syntax backend และทดสอบฟังก์ชันคำนวณจำนวนเงินเป็นสตางค์, กำไร/ขาดทุนรายคัน และข้อมูลวันที่/จำนวนเงินผิดรูปแบบแล้ว แต่ยังไม่ได้ทดสอบการเชื่อมต่อ MongoDB จริงหรือทดสอบ UI ผ่านเบราว์เซอร์ เพราะไม่มีเซิร์ฟเวอร์/แพ็กเกจติดตั้งครบในสภาพแวดล้อมนี้
