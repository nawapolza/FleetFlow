# FleetFlow / Test System — บัญชี สิทธิ์ และมือถือ

- แอดมิน (owner): จัดการสาขา รถ พนักงาน รายการเติมน้ำมัน สต๊อก บัญชีรับจ่าย และรายงานตามเมนูที่มีอยู่ในระบบเดิม; ไม่เปลี่ยนกฎห้ามลบบัญชีตนเองและห้ามปิดสาขาสุดท้าย
- พนักงาน (employee): คำนวณน้ำมัน, บันทึกงานน้ำมัน, ดูและจัดการรายการย้อนหลังภายในสิทธิ์สาขา, ดูสถานะสต๊อกและน้ำมันทุกสาขา; ไม่มีสิทธิ์จัดการบัญชีรถ สต๊อก หรือผู้ใช้
- ทุกบทบาท: เมนู `บัญชีของฉัน` สำหรับเปลี่ยนรหัสผ่านตนเองพร้อมตรวจรหัสเดิม; หลังเปลี่ยนจะต้องเข้าสู่ระบบใหม่
- `/adminnakub`: ยังต้องใช้ `ADMIN_RECOVERY_KEY` ที่ตั้งใน Backend เพื่อสร้าง/กู้คืนบัญชี Admin; URL เพียงอย่างเดียวไม่ใช่การยืนยันตัวตน
- Password ใหม่: หน้าเว็บไม่มีข้อจำกัดจำนวนตัวอักษรขั้นต่ำ/สูงสุด โค้ด Backend ใช้ scrypt รองรับรหัสยาวเกินขีดจำกัด bcrypt เดิม แต่จำกัดขนาดข้อมูลไว้ที่ 4096 ไบต์เพื่อป้องกันการใช้ทรัพยากรเกินควร แนะนำรหัสผ่านที่ยาวและเดายาก
- บัญชีเก่า bcrypt ยังล็อกอินได้ และรหัสผ่านใหม่ที่เปลี่ยนจะเปลี่ยนเป็น scrypt
- `frontend/public/_redirects` ให้ SPA รองรับเส้นทาง `/adminnakub` บนโฮสต์ที่รองรับ `_redirects`; บน Render Static Site ตั้ง Rewrite `/*` ไป `/index.html` ด้วย
- ตั้ง `backend/.env` เป็นค่าจริงในเครื่องหรือ Render Environment และ `frontend/.env` ใส่ `VITE_API_URL=https://BACKEND-DOMAIN/api`; อย่าอัปโหลด secrets ไป GitHub
- วิธีทดสอบ: `cd backend && npm ci && npm start`, อีกหน้าต่าง `cd frontend && npm ci && npm run dev` หรือ `npm run build`
- การเปิดเว็บบนมือถือ: ใช้ dev server ที่เข้าถึงจากมือถือ/HTTPS และตั้ง CORS_ALLOWED_ORIGINS ตาม URL ที่เข้าถึงได้จริง
