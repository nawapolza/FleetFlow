# ขวัญใจดาวทองขนส่ง — Mobile Home & Full Logo Splash

เวอร์ชันนี้ต่อยอดจาก `KWANJAI_DAOTHONG_LOGO_APPLIED_FULL_CODE.zip` โดยคง Backend, MongoDB, `/adminnakub`, ระบบบัญชีรายเที่ยว, และสิทธิ์การใช้งานเดิม

## สิ่งที่ปรับปรุง
- หน้า Dashboard **บนมือถือ** มีฮีโร่แสดงโลโก้จริงที่ผู้ใช้ส่งมาอย่างเด่นชัด พร้อมปุ่มทางลัด 6 เมนู ภาพรวมเที่ยวงาน ระยะทาง ค่าใช้จ่ายน้ำมัน และรายการล่าสุด
- หน้าบันทึกงานของบัญชีพนักงานมีแถบต้อนรับแบบมือถือพร้อมโลโก้จริง
- เปลี่ยนหน้ารอโหลดเป็น Full-screen Splash ที่ใช้ไฟล์ `/kwanjai-logo.png` โดยตรง พร้อมสถานะกรุณารอและแถบโหลดที่ **ไม่แสดงเปอร์เซ็นต์เทียม**
- เปลี่ยนไฟล์โลโก้และไอคอน PWA จากรูปต้นฉบับที่ผู้ใช้ส่งมา โดยรักษาสัดส่วนภาพ
- ดีไซน์เน้นสีแดงเข้ม ทอง ครีม และขาว พร้อมปุ่มสัมผัสขนาดเหมาะกับมือถือและรองรับ reduced-motion
- Desktop Dashboard เดิมยังใช้งานได้ตามปกติ ฟังก์ชันบัญชีรายเที่ยว รายงาน และ Admin ไม่ถูกลบหรือเปลี่ยน API

## อัปเดตขึ้น Render
1. แตก ZIP แล้วนำโฟลเดอร์ `FleetFlow` ขึ้น GitHub (อย่าอัปโหลด `.env` หรือ secret)
2. Deploy Frontend ใหม่ โดย Root Directory=`frontend`, Build Command=`npm ci --include=dev && npm run build`, Publish Directory=`dist`
3. Render Static Site ตั้ง Rewrite `/*` → `/index.html`
4. หากไอคอนติดตั้งบนจอหลักยังเป็นของเก่า ให้ลบชอร์ตคัตเดิมและติดตั้งใหม่เพื่อให้ไอคอนอัปเดต

## หมายเหตุ
- ภาพรวมบนหน้าโฮมอ่านจาก Dashboard API เดิม ไม่เรียก API เพิ่มจากหน้า Mobile Home
- ตัวเลขค่าใช้จ่ายน้ำมันไม่ใช่กำไรสุทธิ เมนูบัญชีรายเที่ยวแยกสำหรับกำไร/ขาดทุน
- ลิงก์ `/adminnakub` ยังมีอยู่และใช้ได้ตามเงื่อนไขของ Backend เดิม
