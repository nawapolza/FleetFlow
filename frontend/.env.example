# Local development: Vite proxies /api to Backend on port 4000
VITE_API_URL=/api
VITE_DEV_BACKEND_URL=http://localhost:4000
# Render Static Site: set VITE_API_URL in Render Environment to your BACKEND Web Service URL (without /api), then deploy. Never point it at the frontend website.
