# Local frontend: copy to frontend/.env and point at your running backend.
VITE_API_URL=http://localhost:4000
# On Render Static Site Environment, use the actual Backend Web Service URL,
# e.g. https://fleetflow-xkuk.onrender.com (no trailing /api for this project).
