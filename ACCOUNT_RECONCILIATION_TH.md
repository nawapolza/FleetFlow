# ระบบบัญชีแยกต้นทุนบิล / รายการกรอกเอง

## วิธีใช้

1. บันทึกงานขนส่งตามเดิม รายได้และต้นทุนในงานย่อยแต่ละงานจะนำมาคิดกำไรในหน้า **บัญชีงานขนส่ง** โดยอ่านจาก `deliveries` โดยตรง ไม่มีการสร้างรายการบัญชีซ้ำใน MongoDB.
2. เปิด **บัญชีงานขนส่ง** เลือกเดือนและทะเบียน บันทึกเฉพาะค่าซ่อม/ค่าใช้จ่ายเพิ่มเติมและรายรับอื่นที่ยังไม่อยู่ในบิล แม้วันนั้นรถไม่ได้วิ่งงานก็บันทึกได้.
3. เปิด **ตรวจสอบรายการย้อนหลัง** รายการเก่าที่วัน/ทะเบียน/ยอดตรงกับต้นทุนหรือรายได้ในบิลจะขึ้นเป็น **อาจซ้ำ** ต้องตรวจสอบก่อนตัดสินใจ.
4. หากยืนยันว่าเป็นรายการที่อยู่ในบิลแล้ว เปิดรายการ กา **รายการนี้บันทึกต้นทุนหรือรายได้อยู่ในบิลแล้ว** เลือกบิลอ้างอิง กดบันทึก ระบบยังเก็บแถวเดิมแต่ไม่นับเข้าเงินรวม เมื่อแก้กลับเป็นรายการแยกต่างหาก จะนับอีกครั้ง.
5. ข้อมูลค่าซ่อมใหม่ที่ไม่อยู่ในบิล ไม่ต้องกาเลือกซ้ำ ระบบจะหักยอดตามปกติ.

## MongoDB

- **ไม่ต้องลบ collection หรือ index ไม่ต้องรัน migration ที่แก้ข้อมูลเดิม** การอัปเดตเวอร์ชันนี้ไม่มีการเปลี่ยนชื่อ collection.
- `deliveries`: ข้อมูลต้นทางของบิลอ่านตรงและคิดผลใหม่ทุกครั้งที่โหลดรายงาน โดยใช้ `delivery._id` และ job id เป็นรหัสอ้างอิงเฉพาะ.
- `transport_ledger`: รายการกรอกเองคงอยู่ตามเดิม และเพิ่มฟิลด์เมื่อบันทึกรายการ: `excluded_from_totals` (boolean), `matched_delivery_id` (string), `matched_job_id` (string), `reconciliation_history` (array บันทึกผู้แก้และเวลา).
- เอกสารเก่าที่ไม่มี `excluded_from_totals` จะถูกนับตามเดิมจน Admin ยืนยันและแก้ไขรายการนั้น ระบบไม่แก้หรือลบข้อมูลเก่าอัตโนมัติ.
- การเชื่อมบิลตรวจสิทธิ์ branch, ทะเบียน, วันที่, งานย่อยจากฐานข้อมูลในฝั่งเซิร์ฟเวอร์ ไม่เชื่อรหัสบิลที่ browser ส่งมาอย่างเดียว.
- ต้องใช้ `MONGO_URI`/`MONGODB_URI` ที่ตั้งอยู่ใน Render Environment เดิม ห้ามนำค่าความลับใส่ Git.

## สูตรบัญชี

`ยอดคงเหลือ = Σ(รายรับในบิล - ต้นทุนในบิล) + Σ(รายรับกรอกเองที่ไม่ซ้ำ) - Σ(รายจ่ายกรอกเองที่ไม่ซ้ำ)`

**ข้อจำกัดสำคัญ:** รายการเดิมที่มีจำนวนเงินเท่ากับบิลอาจเป็นรายการคนละรายการ ระบบจะแจ้งเตือนให้ตรวจสอบ ไม่เปลี่ยนยอดเอง. หากค่าบิลมีการแก้ไขข้ามวันหรือข้ามเดือน ยอดในเดือนที่เลือกจะสะท้อนข้อมูลจากบิลล่าสุด และต้องตรวจสอบรายการกรอกเองที่อ้างอิงบิลนั้นอีกครั้ง.

## การติดตั้ง

Deploy frontend และ backend จากชุดเดียวกัน ทดสอบบนฐานข้อมูลสำรองก่อนและสำรอง MongoDB ก่อนขึ้น production ตรวจ 1) บิลหนึ่งเที่ยว 2) ค่าซ่อมนอกบิล 3) รายการที่ซ้ำแล้วผูกบิล 4) แก้บิลแล้วสรุปยอดใหม่ 5) ตรวจเลขบัญชีย้อนหลัง.
