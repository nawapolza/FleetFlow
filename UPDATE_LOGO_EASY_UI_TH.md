# ขวัญใจดาวทองขนส่ง — โลโก้โปร่งใสและ UI ใช้งานง่าย

- เปลี่ยนโลโก้ทุกหน้าที่อ้างอิง `/kwanjai-logo.png` เป็นไฟล์ PNG พื้นหลังโปร่งใสที่แนบมา โดยใช้ภาพเดิมและไม่วาดตกแต่งเพิ่ม
- ปรับไอคอนแอป PWA 192/512 จากโลโก้เดียวกัน
- เพิ่ม preload ให้โลโก้หลักเพื่อช่วยลดเวลารอรูปในหน้าแรก
- ปรับหน้า Home มือถือให้มีเมนูหลักเรียงลำดับ 1–4 ตัวอักษรใหญ่ขึ้น และปุ่มแตะง่ายขึ้น
- ปรับข้อความ Loading ให้สั้น ชัด และแสดงโลโก้พื้นหลังโปร่งใส
- รวมการแจ้งเตือนสำเร็จแบบ Toast สั้น ๆ ในดีไซน์เดียวกัน และป้องกัน Toast ข้อความซ้ำภายใน 1.3 วินาที
- รวมคำขอ GET ที่เกิดพร้อมกันยังใช้กลไกเดิมใน frontend/src/api.js; เพิ่ม debounce 450ms เฉพาะการอัปเดต Dashboard เมื่อมี Realtime events ติดกัน
- เพิ่ม CSS สำหรับปุ่ม/ฟอร์มมือถือโดยไม่เปลี่ยน API, ฐานข้อมูล, สิทธิ์ผู้ใช้ หรือข้อมูลเก่า

## ติดตั้ง
1. แตกไฟล์ ZIP แล้วเข้าโฟลเดอร์หลัก
2. ตั้งค่า backend/.env ในเครื่องหรือ Environment ของ Render โดยไม่อัปโหลดความลับขึ้น GitHub
3. Frontend บน Render: Root Directory = frontend, Build Command = npm ci --include=dev --no-audit --no-fund && npm run build, Publish Directory = dist
4. Deploy เวอร์ชันใหม่ แล้วหากไอคอนโทรศัพท์ยังเก่าให้ลบ Shortcut เก่าและเพิ่มใหม่

## หมายเหตุ
ไฟล์ชุดนี้คงฟีเจอร์ทั้งหมดจาก ZIP ต้นทางไว้ รวมทั้งข้อมูลบัญชีรายเที่ยวและหน้า /adminnakub. การลด GET ซ้ำเป็นแบบเฉพาะจุด ไม่ได้ลบ API ฝั่ง Backend ที่ยังอาจมีผู้อื่นเรียกใช้งานอยู่
