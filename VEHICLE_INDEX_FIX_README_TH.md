# แก้ไข E11000 vehicles plate:null (ขวัญใจดาวทองขนส่ง)

## ปัญหาที่แก้
ฐานข้อมูลเก่ามี Unique Index `plate_1` บนฟิลด์ `plate` แต่โค้ดปัจจุบันบันทึกเลขทะเบียนไว้ที่ `plate_no` จึงเกิด E11000 `{ plate: null }` เมื่อเพิ่มรถคันที่สอง

- เมื่อ Backend เริ่มเชื่อม MongoDB จะตรวจสอบดัชนีเดิม และลบเฉพาะ Unique Index แบบฟิลด์เดียวบน `vehicles.plate` ที่เลิกใช้แล้ว (ไม่ลบเอกสารรถใด ๆ)
- ระบบพยายามสร้าง Unique Index ใหม่สำหรับ `branch_id + plate_no` เฉพาะรายการรถที่ใช้งานอยู่ (`is_active: 1`) เพื่อป้องกันทะเบียนซ้ำขณะบันทึกพร้อมกัน
- ถ้ามีข้อมูลทะเบียนเก่าที่ซ้ำกันอยู่แล้ว ระบบจะเก็บข้อมูลไว้ตามเดิมและพิมพ์คำเตือนใน Backend Logs เพื่อให้ผู้ดูแลตรวจสอบก่อนแก้ไข ไม่ลบ/รวมรถโดยอัตโนมัติ
- API เปลี่ยนข้อความ E11000 เป็นคำอธิบายภาษาไทยแทนการโชว์ชื่อ Collection และข้อมูลดัชนีแก่ผู้ใช้
- หน้าจัดการรถป้องกันการกดบันทึกซ้ำระหว่างรอคำตอบ
- การเชื่อม MongoDB ครั้งแรกใช้ Promise เดียวร่วมกัน ลดการรัน Migration พร้อมกันหลายคำขอ

## นำขึ้นระบบจริง
1. สำรองฐานข้อมูล MongoDB Atlas ก่อน Deploy โดยเฉพาะ Collection `vehicles` และรายงานรายการ Index เดิม
2. Deploy **Backend ก่อน** เพื่อให้ระบบตรวจดัชนีเก่าและ Migration ทำงาน; บัญชี MongoDB ของ Backend ต้องมีสิทธิ์ `dropIndex` และ `createIndex`
3. ตรวจ Render Backend Logs ว่าการเชื่อมต่อสำเร็จ หากมีคำเตือน `Existing duplicate plates` ให้ตรวจและจัดการข้อมูลซ้ำเดิมด้วยตนเอง
4. Deploy Frontend และทดสอบเพิ่มรถใหม่สองทะเบียน, เพิ่มทะเบียนเดิมซ้ำ, แก้ไข, ปิดใช้งานรถ และบันทึกงานขนส่ง
5. ไม่ต้องเปลี่ยน MongoDB URI หรือ JWT Secret และห้ามนำ `.env` ขึ้น GitHub

## การทดสอบ
`node --check backend/server.js` และ `node scripts/check-vehicle-fix.cjs`

การตรวจสอบแบบ Offline ไม่ใช่การรับรองว่า MongoDB Atlas ของคุณไม่มีข้อมูลซ้ำเดิมหรือปัญหาอื่น ๆ; ต้องทดสอบการทำงานจริงหลัง Deploy
